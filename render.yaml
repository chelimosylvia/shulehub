services:
  - type: web
    name: shulehub-backend
    env: python
    rootDirectory: backend
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app   # adjust if your entry point differs
